import { MessagesPage } from '@/features/community/components/MessagesPage'

export const metadata = { title: 'Mensajes | ZoneKlass' }

export default function MensajesPage() {
  return <MessagesPage />
}
